#!/usr/bin/env bash
# ipc — ipcollect 启动器。自动使用同目录下的 .venv。
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="$HERE/.venv/bin/python"
[ -x "$PY" ] || PY="python3"
export IPC_HOME="$HERE"
exec "$PY" -m ipcollect "$@"
