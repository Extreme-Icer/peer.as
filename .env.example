# 复制为 .env 并填入真实值；.env 已在 .gitignore 中，不会入库。
# 这些变量仅用于部署与本地数据源，运行分析本身不需要它们。

# Cloudflare Pages 部署（wrangler 读取）。account id 可在 CF 控制台获取，
# API token 需有 Pages:Edit 权限。不要把真实值写进任何提交的文件。
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# 可选：覆盖 IP 地理库（ipdb）路径。默认取项目根的 ipdb.txt。
# 官方部署用私有 ipdb；开源复现可指向可再分发的地理库。
IPC_IPDB_PATH=

# 可选：把 Parquet 数据托管到独立宿主(R2/OSS)，实现「前端/数据分离」。
# 留空 ⇒ 数据仍同源走 dist/data（默认）。详见 AGENTS.md「数据分离托管(R2)」。
# VITE_DATA_BASE: 海外前端拉数据的公开根 URL(构建期注入, 经 vite envDir 读本文件)。
#   含账号相关的 pub-<hash>.r2.dev 串属非公开 id ⇒ 只放这里, 勿写进提交文件/AGENTS.md。
#   例: https://pub-xxxxxxxx.r2.dev 或自定义域名 https://data.peer.as
VITE_DATA_BASE=
# R2_BUCKET: 上传脚本用的桶名(wrangler r2 ... 命令读取)。
R2_BUCKET=

# ── 中国优化(见 AGENTS.md「中国优化(cn.peer.as)」) ───────────────────────────
# 前端在运行时按 /cdn-cgi/trace loc=CN 把数据/wasm 源切到中国优化 VPS。以下三项可选：
# VITE_CN_BASE: CN 数据/wasm 宿主源(构建期注入)。留空 ⇒ 默认 https://cn.peer.as。
#   该源须 /data(Parquet, 支持 Range+CORS) + /duckdb(自托管 DuckDB-WASM bundle)。
VITE_CN_BASE=
# CN_DEPLOY_SSH / CN_DEPLOY_PATH: daily-refresh.sh 把 dist/data rsync 到 VPS 用。
#   含机器 IP ⇒ 只放这里(脱敏)。留空 ⇒ 跳过 VPS 同步。例: root@198.51.100.10 + /var/www/cn
CN_DEPLOY_SSH=
CN_DEPLOY_PATH=
